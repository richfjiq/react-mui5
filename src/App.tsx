import { Button, Container } from '@mui/material';

function App() {
  return (
    <Container maxWidth="xl">
      <Button variant="contained" fullWidth>
        Hello World
      </Button>
    </Container>
  );
}

export default App;
